```es6
let m = {
	num:0,
	valueOf:function() {
		this.num+=1;
		return this.num%2 ? "m" : "n";
    	}
}

m.valueOf()==="m"&&m.valueOf()==="n"
```

箭头函数this的问题：这里指向了window
```es6
let m = {
	num:0,
	valueOf:() => {
		console.log(this)
    	}
}
```
